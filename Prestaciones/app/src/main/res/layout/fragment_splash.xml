<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/white"
    android:gravity="center"
    android:orientation="vertical"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    >

    <RelativeLayout
        android:id="@+id/ll_splash"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center">

        <ImageView
            android:id="@+id/splash_app_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="10dp"
            android:layout_centerInParent="true"
            android:src="@drawable/ic_app_splash"/>

        <TextView
            android:id="@+id/splash_loading"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/item_text"
            android:layout_below="@+id/splash_app_icon"
            android:textAlignment="center"
            android:textSize="18dp"
            android:text="@string/loading"/>

        <ProgressBar
            android:layout_below="@+id/splash_loading"
            android:layout_marginTop="5dp"
            android:id="@+id/progress_bar_splash"
            android:layout_centerHorizontal="true"
            android:background="@drawable/shape_progress_bar"
            android:progressDrawable="@drawable/layerlist_progress_bar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="200dp"
            android:layout_height="8dp" />
    </RelativeLayout>

</LinearLayout>